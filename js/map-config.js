/* map-config.js — 지도 타일 키.
   ─────────────────────────────────────────────────────────────────────────
   ★★키가 **필수**다. 없으면 지도 판이 빈 채로 남는다(다른 타일 제공처는 전부 걷어냈다 —
     폴백을 두려니 코드가 셋으로 갈라져서, 키가 생긴 뒤로는 복잡하기만 했다).
     키가 없거나 막히면 지도 아래에 그렇게 적힌다 — 조용히 비워 두지 않는다.

   MapTiler 키 넣는 법
     1. https://cloud.maptiler.com 가입 (무료 플랜)
     2. Keys → 키 하나 만들기
     3. ★그 키의 **Allowed origins** 에 배포 주소를 넣는다:
          https://thpht-trip.vercel.app
        안 넣으면 남이 우리 키로 타일을 받아 쓸 수 있다. 이 키는 브라우저에 노출되는
        공개 키라서(Supabase anon 키와 같은 성격) **출처 제한이 유일한 방어선이다.**
     4. 아래 따옴표 안에 붙여넣는다.

   무엇이 좋아지나
     · 위성이 MapTiler `hybrid` 가 된다 — 도로·지명이 **처음부터 얹혀 나오는** 스타일이라
       우리가 OSM 을 혼합해서 흉내 내던 것보다 훨씬 깨끗하다.
     · '지도' 는 `dataviz` 가 된다 — 데이터를 얹으라고 만든 옅은 스타일이라
       회색조 필터를 걸 필요가 없다. */
/* ★이 키는 **출처 제한**으로 막혀 있다(2026-09-01 확인: thpht-trip.vercel.app 만 200,
   그 밖의 출처와 출처 없는 요청은 전부 403). 브라우저에 노출되는 공개 키라
   그 제한이 유일한 방어선이다 — 도메인이 바뀌면 MapTiler 대시보드에서 같이 고칠 것.
   ★localhost 는 허용 목록에 없다. 로컬에서 지도를 보려면 대시보드에 한 줄 더 넣거나,
     그냥 배포본에서 확인한다(앱의 다른 부분은 로컬에서 다 돈다). */
const MAPTILER_KEY = 'GPHoCZCBF7BTYygsmwzy';
